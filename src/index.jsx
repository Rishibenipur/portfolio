import './index.css';
import App from './App';

// const root = ReactDOM.createRoot(document.getElementById('root'));

const abc = () => {
  return (
    <div>
      <App />
    </div>
  );
};

export default abc;
